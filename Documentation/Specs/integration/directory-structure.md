# Directory Structure Specification

> Every file and directory in the rebuilt Delightful Design System repo, with its purpose, whether it is authored or generated, and whether it is a consumer entry point.

Cross-references: [[build-pipeline]], [[cascade-layers]], [[tokens/palette-schema]]

---

## 1. Root Layout

```
delightful/
├── palettes/               ← Color source of truth
├── emitters/               ← Build scripts (palette → outputs)
├── src/                    ← Authored CSS/JS source files
├── docs/                   ← Assembled documentation pages
├── ports/                  ← Generated platform outputs
├── packages/               ← npm package wrappers
├── tests/                  ← Playwright test suite
├── scripts/                ← Utility scripts
├── .github/                ← CI/CD workflows
├── package.json            ← npm workspaces root
├── biome.json              ← Linter config
├── playwright.config.ts    ← Test runner config
├── CLAUDE.md               ← Repo conventions for agents
├── README.md
├── CHANGELOG.md
└── LICENSE
```

---

## 2. `palettes/` — Color Authority

```
palettes/
├── delightful.json         ← AUTHORED · COLOR AUTHORITY
└── palette.schema.json     ← AUTHORED · JSON Schema validation
```

| File | Authored/Generated | Purpose |
|------|-------------------|---------|
| `delightful.json` | Authored | The single source of truth for all color. Contains primitives, semantic mappings (light/dark), and terminal hex values. EDIT THIS to change any color. |
| `palette.schema.json` | Authored | JSON Schema (draft 2020-12) used by ajv to validate `delightful.json` before any emitter runs. |

---

## 3. `emitters/` — Build Pipeline

```
emitters/
├── orchestrator.mjs        ← AUTHORED · Pipeline entry point
├── validate.mjs            ← AUTHORED · Schema + WCAG validation
├── css.mjs                 ← AUTHORED · palette → tokens.css
├── vscode.mjs              ← AUTHORED · palette → VS Code themes
├── obsidian.mjs            ← AUTHORED · palette → Obsidian theme
├── ghostty.mjs             ← AUTHORED · palette → Ghostty config
├── iterm2.mjs              ← AUTHORED · palette → iTerm2 colors
├── starship.mjs            ← AUTHORED · palette → Starship TOML
├── tailwind.mjs            ← AUTHORED · palette → Tailwind preset
├── palette-loader.mjs      ← AUTHORED · Shared: read + validate palette JSON
└── oklch-utils.mjs         ← AUTHORED · Shared: OKLCH parsing, hex conversion (culori)
```

All emitters implement the same interface:
```js
emit(palette, options) → { files: [{ path, content }], warnings: [] }
```

Emitters are pure functions — no file I/O. The orchestrator handles all writes.

---

## 4. `src/` — Authored Source Files

```
src/
├── tokens.css              ← GENERATED by css.mjs · DO NOT EDIT
├── foundation.css          ← AUTHORED · Tier 3 foundation tokens
├── reset.css               ← AUTHORED · CSS reset + layer declaration
├── utilities.css           ← AUTHORED · Utility classes
│
├── motion/
│   └── motion.css          ← AUTHORED · 59 keyframes + animation utilities
│
├── animation/              ← AUTHORED · JS physics modules (out of scope for v1 docs)
│   ├── spring.js
│   ├── flip.js
│   ├── particles.js
│   └── index.js
│
└── components/
    ├── index.css           ← AUTHORED · @import bundle entry for all components
    ├── badge.css           ← AUTHORED
    ├── avatar.css          ← AUTHORED
    ├── tooltip.css         ← AUTHORED
    ├── empty-state.css     ← AUTHORED
    ├── divider.css         ← AUTHORED
    ├── notification-badge.css ← AUTHORED
    ├── button.css          ← AUTHORED
    ├── card.css            ← AUTHORED
    ├── tile.css            ← AUTHORED
    ├── input.css           ← AUTHORED
    ├── textarea.css        ← AUTHORED
    ├── select.css          ← AUTHORED
    ├── multi-select.css    ← AUTHORED
    ├── checkbox.css        ← AUTHORED
    ├── radio.css           ← AUTHORED
    ├── toggle.css          ← AUTHORED
    ├── range.css           ← AUTHORED
    ├── topnav.css          ← AUTHORED
    ├── tabs.css            ← AUTHORED
    ├── breadcrumbs.css     ← AUTHORED
    ├── pagination.css      ← AUTHORED
    ├── sidebar.css         ← AUTHORED
    ├── segmented-control.css ← AUTHORED
    ├── command-palette.css ← AUTHORED
    ├── skip-link.css       ← AUTHORED
    ├── table.css           ← AUTHORED
    ├── stepper.css         ← AUTHORED
    ├── scroll-progress.css ← AUTHORED
    ├── back-to-top.css     ← AUTHORED
    ├── toast.css           ← AUTHORED
    ├── alert.css           ← AUTHORED
    ├── modal.css           ← AUTHORED
    ├── drawer.css          ← AUTHORED
    ├── progress.css        ← AUTHORED
    ├── popover.css         ← AUTHORED
    ├── code-block.css      ← AUTHORED
    ├── skeleton.css        ← AUTHORED
    ├── accordion.css       ← AUTHORED
    ├── dropdown.css        ← AUTHORED
    ├── bento-grid.css      ← AUTHORED
    ├── blur-grid.css       ← AUTHORED
    ├── tilt-card.css       ← AUTHORED
    ├── spotlight.css       ← AUTHORED
    ├── magnetic-button.css ← AUTHORED
    ├── sidebar-layout.css  ← AUTHORED
    └── staggered-reveal.css ← AUTHORED
```

**Consumer entry points in `src/`**:
- `src/reset.css` — import first; contains the layer order declaration
- `src/tokens.css` — import first or alongside reset; contains all color custom properties
- `src/foundation.css` — import after tokens
- `src/components/index.css` — import for full component bundle
- `src/components/{name}.css` — import individual components
- `src/motion/motion.css` — import if animations are needed
- `src/utilities.css` — import last (highest layer priority)

---

## 5. `docs/` — Assembled Documentation

```
docs/
├── index.html              ← ASSEMBLED · Main showcase (all 47 components)
├── color.html              ← ASSEMBLED · Palette explorer
├── motion.html             ← ASSEMBLED · Animation catalog (59 keyframes)
├── animation.html          ← ASSEMBLED · Physics demos
├── preview-light.html      ← ASSEMBLED · 1280×720 screenshot target (light)
└── preview-dark.html       ← ASSEMBLED · 1280×720 screenshot target (dark)
```

Documentation pages IMPORT source CSS/JS files — they do not define tokens or components inline. They are assembled from source, not a source themselves.

---

## 6. `ports/` — Generated Platform Outputs

```
ports/
├── vscode/
│   ├── themes/
│   │   ├── delightful-light-color-theme.json   ← GENERATED
│   │   └── delightful-dark-color-theme.json    ← GENERATED
│   ├── package.json                            ← AUTHORED
│   └── README.md                               ← AUTHORED
│
├── obsidian/
│   ├── theme.css                               ← GENERATED
│   ├── manifest.json                           ← AUTHORED
│   └── README.md                               ← AUTHORED
│
├── ghostty/
│   ├── delightful-light.conf                   ← GENERATED
│   └── delightful-dark.conf                    ← GENERATED
│
├── iterm2/
│   ├── Delightful Light.itermcolors            ← GENERATED
│   └── Delightful Dark.itermcolors             ← GENERATED
│
├── starship/
│   └── starship.toml                           ← GENERATED
│
└── claude-plugin/
    ├── tokens.css                              ← GENERATED (copy of src/tokens.css)
    ├── skills/                                 ← AUTHORED
    └── agents/                                 ← AUTHORED
```

Generated files in `ports/` are overwritten on every `npm run build`. Authored files (`package.json`, `manifest.json`, `README.md`) are never touched by the build.

---

## 7. `packages/` — npm Package Wrappers

```
packages/
├── tokens/                     ← @delightful/tokens
│   ├── package.json            ← AUTHORED · Package manifest with exports map
│   ├── dist/
│   │   ├── tokens.css          ← GENERATED · Copy of src/tokens.css
│   │   ├── tokens.json         ← GENERATED · W3C DTCG format
│   │   ├── tokens.mjs          ← GENERATED · ES module export
│   │   └── tokens.d.ts         ← GENERATED · TypeScript declarations
│   └── README.md               ← AUTHORED
│
├── css/                        ← @delightful/css
│   ├── package.json            ← AUTHORED
│   ├── dist/
│   │   ├── components.css      ← GENERATED · Full component bundle
│   │   ├── components/         ← GENERATED · Individual component files
│   │   │   ├── button.css
│   │   │   ├── card.css
│   │   │   └── ... (one per component)
│   │   ├── foundation.css      ← GENERATED · Copy of src/foundation.css
│   │   ├── motion.css          ← GENERATED · Copy of src/motion/motion.css
│   │   └── reset.css           ← GENERATED · Copy of src/reset.css
│   └── README.md               ← AUTHORED
│
└── tailwind/                   ← @delightful/tailwind
    ├── package.json            ← AUTHORED
    ├── dist/
    │   └── preset.js           ← GENERATED · Tailwind preset with var() references
    └── README.md               ← AUTHORED
```

### Package Exports Maps

**`@delightful/tokens`**:
```json
{
  "exports": {
    ".": {
      "import": "./dist/tokens.mjs",
      "require": "./dist/tokens.cjs",
      "types": "./dist/tokens.d.ts"
    },
    "./css": "./dist/tokens.css",
    "./json": "./dist/tokens.json"
  }
}
```

**`@delightful/css`**:
```json
{
  "exports": {
    ".": "./dist/components.css",
    "./components/*": "./dist/components/*.css",
    "./foundation": "./dist/foundation.css",
    "./motion": "./dist/motion.css",
    "./reset": "./dist/reset.css"
  }
}
```

**`@delightful/tailwind`**:
```json
{
  "exports": {
    ".": "./dist/preset.js"
  }
}
```

---

## 8. `tests/` — Test Suite

```
tests/
├── palette.spec.ts         ← Schema validation + WCAG contrast
├── tokens.spec.ts          ← CSS emitter output correctness
├── ports.spec.ts           ← Cross-port palette consistency
├── motion.spec.ts          ← Keyframes + reduced-motion
├── docs.spec.ts            ← Documentation pages render correctly
└── components/
    ├── badge.spec.ts
    ├── button.spec.ts
    ├── card.spec.ts
    ├── input.spec.ts
    ├── toggle.spec.ts
    ├── toast.spec.ts
    ├── modal.spec.ts
    ├── table.spec.ts
    ├── accordion.spec.ts
    ├── command-palette.spec.ts
    └── ... (one file per component or per batch for simpler ones)
```

---

## 9. `scripts/` — Utility Scripts

```
scripts/
└── bump-version.mjs        ← AUTHORED · Atomic version bump across all packages
```

`bump-version.mjs` updates the version string in:
1. `package.json` (root)
2. `packages/tokens/package.json`
3. `packages/css/package.json`
4. `packages/tailwind/package.json`
5. `palettes/delightful.json` (the `version` field)
6. Creates an annotated git tag `v{version}`

---

## 10. Root Config Files

| File | Purpose |
|------|---------|
| `package.json` | npm workspaces root. Defines `scripts.build`, `scripts.test`, `scripts.lint`, `workspaces` array pointing to `packages/*` |
| `biome.json` | Linter + formatter config. Enforces no-primitive-reference rule in component CSS. |
| `playwright.config.ts` | Playwright config: Chromium, single worker, HTML reporter, timeout settings |
| `CLAUDE.md` | Agent instructions for this repo: architecture overview, coding conventions, what not to do |
| `.gitignore` | Ignores: `node_modules/`, `dist/`, `test-results/`, `playwright-report/`, `*.itermcolors` (optional — they're generated) |
| `LICENSE` | MIT |

---

## 11. Authored vs Generated Summary

| Category | Authored (edit these) | Generated (never edit) |
|----------|----------------------|----------------------|
| Color data | `palettes/delightful.json` | `src/tokens.css`, all `ports/` outputs |
| Emitters | All `emitters/*.mjs` | — |
| CSS source | `src/foundation.css`, `src/reset.css`, `src/utilities.css`, `src/motion/motion.css`, `src/components/*.css` | — |
| Documentation | `docs/*.html` (template structure) | (CSS/JS injected from src imports) |
| Tests | All `tests/*.spec.ts` | — |
| Packages | `packages/*/package.json`, `packages/*/README.md` | `packages/*/dist/*` |
| Config | `biome.json`, `playwright.config.ts`, `package.json` | — |
