# Delightful — Zsh Additions
# ===========================
# Add these to your ~/.zshrc. Each section is independent.

# --- Starship Prompt ---
# Install: brew install starship (macOS) or see https://starship.rs
# Config: copy starship.toml to ~/.config/starship.toml
eval "$(starship init zsh)"

# --- Ghostty Quick Terminal → Claude Code ---
# Ghostty 1.3+ sets GHOSTTY_QUICK_TERMINAL=1 in quick terminal sessions.
# On 1.2.x this block is inert — type `cc` manually on first open.
if [[ "$GHOSTTY_QUICK_TERMINAL" == "1" ]]; then
  claude --dangerously-skip-permissions
fi

# --- Zsh Defaults ---
setopt AUTO_CD
setopt CORRECT
setopt HIST_IGNORE_ALL_DUPS
setopt SHARE_HISTORY
HISTSIZE=50000
SAVEHIST=50000
HISTFILE=~/.zsh_history

autoload -Uz compinit && compinit
zstyle ':completion:*' matcher-list 'm:{a-z}={A-Z}'
zstyle ':completion:*' menu select
zstyle ':completion:*' list-colors ''

# --- AI CLI Aliases ---
# Each alias clears the visible screen (preserving scrollback) for a clean start.
# Claude Code
alias c='clear -x && claude'
alias cc='clear -x && claude --dangerously-skip-permissions'
alias cr='clear -x && claude --resume'
alias ccr='clear -x && claude --dangerously-skip-permissions --resume'

# Codex
alias x='clear -x && codex'
alias xx='clear -x && codex --full-auto'
alias xr='clear -x && codex resume'
alias xxr='clear -x && codex --full-auto resume'

# Gemini
alias g='clear -x && gemini'
alias gg='clear -x && gemini --yolo'
alias gr='clear -x && gemini --resume latest'
alias ggr='clear -x && gemini --yolo --resume latest'

